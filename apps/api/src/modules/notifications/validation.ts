// import { z } from 'zod';

/**
 * Notifications Validation Schemas
 *
 * Zod schemas for request validation in the notifications module.
 */

// Example:
// export const createNotificationsSchema = z.object({
//   name: z.string().min(1),
// });

export {};
