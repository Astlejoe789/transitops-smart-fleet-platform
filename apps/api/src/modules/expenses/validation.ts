// import { z } from 'zod';

/**
 * Expenses Validation Schemas
 *
 * Zod schemas for request validation in the expenses module.
 */

// Example:
// export const createExpensesSchema = z.object({
//   name: z.string().min(1),
// });

export {};
