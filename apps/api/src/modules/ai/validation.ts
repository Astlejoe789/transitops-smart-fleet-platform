// import { z } from 'zod';

/**
 * Ai Validation Schemas
 *
 * Zod schemas for request validation in the ai module.
 */

// Example:
// export const createAiSchema = z.object({
//   name: z.string().min(1),
// });

export {};
