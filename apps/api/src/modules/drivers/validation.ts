// import { z } from 'zod';

/**
 * Drivers Validation Schemas
 *
 * Zod schemas for request validation in the drivers module.
 */

// Example:
// export const createDriversSchema = z.object({
//   name: z.string().min(1),
// });

export {};
