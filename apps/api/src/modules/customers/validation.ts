// import { z } from 'zod';

/**
 * Customers Validation Schemas
 *
 * Zod schemas for request validation in the customers module.
 */

// Example:
// export const createCustomersSchema = z.object({
//   name: z.string().min(1),
// });

export {};
