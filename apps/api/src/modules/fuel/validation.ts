// import { z } from 'zod';

/**
 * Fuel Validation Schemas
 *
 * Zod schemas for request validation in the fuel module.
 */

// Example:
// export const createFuelSchema = z.object({
//   name: z.string().min(1),
// });

export {};
