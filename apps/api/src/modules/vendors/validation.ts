// import { z } from 'zod';

/**
 * Vendors Validation Schemas
 *
 * Zod schemas for request validation in the vendors module.
 */

// Example:
// export const createVendorsSchema = z.object({
//   name: z.string().min(1),
// });

export {};
