// import { z } from 'zod';

/**
 * Analytics Validation Schemas
 *
 * Zod schemas for request validation in the analytics module.
 */

// Example:
// export const createAnalyticsSchema = z.object({
//   name: z.string().min(1),
// });

export {};
