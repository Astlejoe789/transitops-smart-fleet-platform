// import { z } from 'zod';

/**
 * Dashboard Validation Schemas
 *
 * Zod schemas for request validation in the dashboard module.
 */

// Example:
// export const createDashboardSchema = z.object({
//   name: z.string().min(1),
// });

export {};
