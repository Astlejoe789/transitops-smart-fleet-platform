// import { z } from 'zod';

/**
 * Administration Validation Schemas
 *
 * Zod schemas for request validation in the administration module.
 */

// Example:
// export const createAdministrationSchema = z.object({
//   name: z.string().min(1),
// });

export {};
