// import { z } from 'zod';

/**
 * Reports Validation Schemas
 *
 * Zod schemas for request validation in the reports module.
 */

// Example:
// export const createReportsSchema = z.object({
//   name: z.string().min(1),
// });

export {};
