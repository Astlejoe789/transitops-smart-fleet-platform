/**
 * Shared UI components barrel.
 *
 * Subdirectories:
 * - ui/       → shadcn/ui components (auto-generated via `npx shadcn@latest add`)
 * - common/   → Reusable application components (e.g., DataTable, StatusBadge)
 * - forms/    → Form field components (e.g., FormInput, FormSelect)
 * - layout/   → Layout primitives (e.g., PageHeader, Section, Container)
 */

export {};
