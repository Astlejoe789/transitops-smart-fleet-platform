# Shared Packages

This directory is reserved for shared packages across the monorepo.

Potential packages:
- `@transitops/shared-types` — Shared TypeScript types between frontend and backend
- `@transitops/validators` — Shared Zod validation schemas
- `@transitops/constants` — Shared constants
